# 学习资料与技术栈标准

## 材料优先级（硬规则）

1. **文档为主**：现行官方文档 + 当天 `NOTES.md` + 仓库可运行代码（可保证跟现行版本对齐）
2. **视频为辅**：只有文档绕不过去、需要演示时才看；视频无法保证永远最新
3. **冲突时**：永远以官方文档和本仓库代码为准，不采用旧视频里的过时 API

## 技术默认

- FastAPI 现行文档写法
- **Pydantic v2**（例如用 `model_dump()`，不用已弃用的 v1 `.dict()` 当默认）
- 数据库阶段默认 **SQLAlchemy 2**
- 禁止把多年未更新的长课当主教材

## 每天怎么用材料

先读文档与 NOTES →（可选）短视频 → 按 README 写代码 → push → 老师 review。
