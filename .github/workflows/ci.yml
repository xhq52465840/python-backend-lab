name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  week1-tests:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: week1-fastapi-crud
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        run: pytest -q
